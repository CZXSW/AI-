# CIFAR-10 图像分类实验报告

## 摘要（Abstract）
本实验在 CIFAR-10 数据集上完成了基础网络训练、损失曲线绘制、正则化实验、超参数对比和自定义网络实现。实验结果显示：学习率会明显影响收敛速度和准确率，自定义网络在本次实验中准确率最高。

## 引言（Introduction）
这次作业的目标是用深度学习方法做一个 10 分类图像识别任务，并了解模型训练和测试的基本流程。

我先复现了给出的基础 LeNet 网络，然后按任务要求依次完成了损失可视化、加入正则化、调整学习率和设计新网络。

通过这些实验，我重点观察了损失变化和测试准确率变化，并对不同设置的效果做了简单分析。

## 方法（Methodology）
1. **数据集与预处理**：使用 CIFAR-10，图像转为张量并做归一化。
2. **基础模型**：使用 LeNet（2个卷积层+3个全连接层），损失函数为交叉熵，优化器为 SGD。
3. **Task1**：在训练中记录每个 batch 的 loss，并画出损失曲线。
4. **Task2**：在第一个全连接层和第二个全连接层之间加入 Dropout（p=0.3），并在优化器加入 L2 正则（weight_decay=1e-4）。
5. **Task3**：对比不同学习率（0.0005、0.001、0.005）的训练结果。
6. **Task4**：实现一个更现代的 CNN（更多卷积层，含 BatchNorm 和 Dropout），并和基础模型比较。

## 实验（Experiments）
### 1）实验设置
- 框架：PyTorch
- 训练轮数：基础实验 5 个 epoch（Task3 为 3 个 epoch 对比）
- 结果指标：测试集准确率（Accuracy）和训练损失（Loss）

### 2）实验结果与分析
- **基础 LeNet（Task1）**：测试准确率 **60.85%**，训练损失整体下降（第5轮平均 loss 约 **1.004**），说明模型在学习。  
  训练集上的损失更小不一定代表测试效果更好，可能会出现过拟合，所以还要看测试集准确率。
- **Task2（Dropout + L2）**：测试准确率 **57.58%**，第5轮平均 loss 约 **1.229**（Dropout p=0.3，weight_decay=1e-4）。  
  这次结果比基础模型低，说明这组正则化参数还需要继续调。
- **Task3（学习率对比，3个epoch）**：  
  - lr=0.0005：最终平均 loss **1.2835**，测试准确率 **55.71%**  
  - lr=0.001：最终平均 loss **1.1997**，测试准确率 **58.49%**  
  - lr=0.005：最终平均 loss **1.6175**，测试准确率 **44.11%**  
  从结果看，0.001 在这次实验里最合适；0.005 偏大，训练不稳定。
- **Task4（自定义网络）**：测试准确率 **68.32%**，第5轮平均 loss 约 **0.839**，明显高于基础 LeNet（60.85%）。

## 总结（Conclusion）
本实验完成了四个子任务。结果表明：学习率对模型效果影响很大；在本次参数设置下，简单加入 Dropout 和 L2 没有提升准确率，后续还可以继续调参；自定义 CNN 的准确率最高，但训练成本也更高。
